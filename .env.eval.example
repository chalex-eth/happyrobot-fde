# Optional overlay for native adversarial and negotiation controllers.
# Load after .env.local; never load this overlay for the normal Docker stack.
ADVERSARIAL_MCP_ENABLED=true
ADVERSARIAL_MCP_TOKEN=
