export default function Home() {
  return <main><h1>Carrier sales — local TMS checkpoint</h1><p>The authenticated diagnostic endpoint is ready for real read-only TMS checks.</p><p>Run <code>npm run verify:local</code> from the project directory. See the README for setup and results.</p></main>;
}
