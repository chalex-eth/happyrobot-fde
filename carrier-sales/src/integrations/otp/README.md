# OTP adapter — not implemented

Select the HappyRobot-managed sender first, then documented alternatives.
OtpSender will wrap a real provider. Use a preconfigured tester-owned destination,
not caller-supplied contact information. Keep codes out of model context and
logs. Test sinks are test-only and do not satisfy live acceptance.
