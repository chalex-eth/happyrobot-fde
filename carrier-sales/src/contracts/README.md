# Runtime contracts

Start every external boundary with unknown input and Zod validation. Infer types
from schemas. Keep private TMS loads separate from public tool and manager DTOs.
The initial identifier and integer-cent schemas are primitives, not approved
external contracts. Tool inputs, public outputs and booking-result unions are
pending specification review.
